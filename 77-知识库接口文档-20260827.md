# 77 服务器知识库接口文档（重整版）

> 适用范围：77 服务器当前知识库、Gateway、平台代理与工作流调用。  
> 结论先说：知识库不是“挂了”，而是现在已经按两条线拆开了：
> 1. `public` 匿名只读线：只读目录、文件和检索；
> 2. `authenticated` 业务线：保留数据集、文档、解析、检索等完整能力，需要平台 Token。

---

## 1. 当前状态说明

根据当前 77 的运行与测试记录：

- 知识库当前主目录：`umc`
- 目录/数据集 ID：`34a89aa7b43b473d8326cf6540fc3894`
- 当前知识检索支持：
  - `bm25`
  - `vector`
  - `graph`
- 现在的测试口径里，`vector` 侧已经有“单路标记”，不要把测试日志里的“vector 单路”误读成“三路都跑完了”
- 如果接口直接返回 `[]` 或空结果，通常是：
  - `folder_id` 没传对
  - `platform token` 没传对
  - 路由打到了错误前缀
  - 上游 `MailGraph` / `Gateway` / `Milvus` 不可用

---

## 2. 推荐访问入口

### 2.1 通过 18085 访问

```text
http://77.242.240.158:18085/api/platform/api/v1
```

### 2.2 18085 上的 Swagger

```text
http://77.242.240.158:18085/api/platform/api/v1/docs
```

### 2.3 18085 上的 OpenAPI

```text
http://77.242.240.158:18085/api/platform/api/v1/openapi.json
```

---

## 3. 匿名只读接口（public）

这组接口不需要 `Authorization`，但只允许读。

### 3.1 获取目录树

```http
GET /public/knowledge/folders/tree
```

示例：

```bash
curl -sS \
  'http://77.242.240.158:18085/api/platform/api/v1/public/knowledge/folders/tree'
```

### 3.2 列出文件元数据

```http
GET /public/knowledge/files?folder_id=<folder_id>&recursive=false
```

示例：

```bash
curl -sS \
  'http://77.242.240.158:18085/api/platform/api/v1/public/knowledge/files?folder_id=34a89aa7b43b473d8326cf6540fc3894'
```

### 3.3 分页列出文件元数据

```http
GET /public/knowledge/files/page?folder_id=<folder_id>&recursive=false&page=1&page_size=20
```

### 3.4 匿名检索

```http
POST /public/knowledge/search
Content-Type: application/json
```

请求体：

```json
{
  "query": "media regulation",
  "folder_id": "34a89aa7b43b473d8326cf6540fc3894",
  "top_k": 3
}
```

示例：

```bash
curl -sS -X POST \
  -H 'Content-Type: application/json' \
  -d '{"query":"media regulation","folder_id":"34a89aa7b43b473d8326cf6540fc3894","top_k":3}' \
  'http://77.242.240.158:18085/api/platform/api/v1/public/knowledge/search'
```

### 3.5 public 接口约束

- 不需要 `Authorization`
- `folder_id` 必填
- 只允许读目录、文件、检索结果
- 不开放上传、解析、删除、目录管理、原始文件下载

---

## 4. 认证业务接口（authenticated）

这组接口仍然需要平台 Token：

```http
Authorization: Bearer <platform_token>
Content-Type: application/json
```

### 4.1 列出数据集

```http
GET /api/knowledge/datasets
```

### 4.2 获取目录下文档列表

```http
GET /api/knowledge/datasets/{dataset_id}/documents
```

### 4.3 上传文档

```http
POST /api/knowledge/datasets/{dataset_id}/documents
```

FormData：

- `files`: 文件数组
- `parent_path`: 可选，如 `/umc`

### 4.4 解析文档

```http
POST /api/knowledge/datasets/{dataset_id}/documents/parse
```

### 4.5 获取单个文档

```http
GET /api/knowledge/datasets/{dataset_id}/documents/{document_id}
```

### 4.6 单目录检索

```http
POST /api/knowledge/datasets/{dataset_id}/search
```

最小请求体：

```json
{
  "query": "book content license",
  "top_k": 3
}
```

### 4.7 全局知识检索

```http
POST /api/knowledge/search
```

推荐请求体：

```json
{
  "dataset_ids": ["34a89aa7b43b473d8326cf6540fc3894"],
  "expected_versions": {
    "34a89aa7b43b473d8326cf6540fc3894": "mailgraph-4e372c337caeaca52b86f53055b550ab"
  },
  "query": "book content license",
  "retrieval_modes": ["bm25", "vector", "graph"],
  "candidate_k": 30,
  "top_k": 8,
  "rerank": {
    "enabled": false,
    "top_n": 8
  }
}
```

---

## 5. 检索参数含义

| 参数 | 含义 |
|---|---|
| `retrieval_modes` | 召回通道，支持 `bm25`、`vector`、`graph` |
| `candidate_k` | 每个召回通道的候选上限，不是总数 |
| `top_k` | 最终返回证据条数 |
| `rerank.enabled` | 是否启用重排 |
| `rerank.top_n` | 开启重排后的最终返回数 |

---

## 6. 常见返回码

| 状态码 | 含义 |
|---|---|
| 200 | 成功 |
| 400 | 请求体格式错误 |
| 401 | 未登录/无效 Token |
| 403 | 没权限 |
| 404 | 路径或目录不存在 |
| 422 | 参数校验失败 |
| 502 | 上游 MailGraph/Gateway 不可用 |
| 503 | 上游检索/重排服务暂时不可用 |

---

## 7. 当前建议

1. 如果你是给前端/工作流用，优先走 `public/knowledge/search`。
2. 如果你要看真实目录、文件元数据、上传、解析，走认证业务接口。
3. 如果页面只显示“vector 单路标记”，不要误认为三路都已经执行完毕。
4. 如果需要，我可以把这份文档再整理成：
   - Excel 版接口清单
   - Markdown 详细版
   - 按“申请详情 / ISBN 查询 / 新增申请”拆分的工作流调用版

