/** Shared upload count limits used by attachment fields. */
export const UPLOAD_LIMITS = {
  /** Only one active file is allowed for a single-attachment field. */
  SINGLE_ATTACHMENT: 1,
} as const;
